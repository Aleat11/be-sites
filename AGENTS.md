# House rules

- Never rewrite published git history: no force-push, rebase, amend, or squash of commits already on `main`.
- Commits pushed to `main` sync to the connected editor — keep the branch in a working state.
- The site deploys to Vercel (`be-sites-web`); verify the build passes before pushing.
